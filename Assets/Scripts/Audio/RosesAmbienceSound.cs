using UnityEngine;
using FMOD.Studio;

public class RosesAmbienceSound : MonoBehaviour
{
    void Start()
    {
        //AudioManager.instance.PlayOneShot(FMODEvents.instance.playerFootsteps, this.transform.position);
    }
}
